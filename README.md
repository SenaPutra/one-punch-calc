# One‑Punch Effort Calculator (Split Files)

Buka `index.html`, atau build Docker:
```bash
docker build -t onepunch-effort .
docker run -d --name onepunch-effort -p 8069:80 onepunch-effort
```
Nginx reverse proxy prefix `/babitampanandfren`: lihat `nginx.onepunch.conf`.
